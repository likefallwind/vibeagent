#!/usr/bin/env python3
import sys
from pathlib import Path

source_root = Path(__file__).resolve().parents[1]
if (source_root / "vibeagent").is_dir():
    sys.path.insert(0, str(source_root))

from vibeagent.cli import main

raise SystemExit(main(sys.argv[1:]))
